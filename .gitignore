node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo
coverage

# Harvest Checkpoint 4: real-browser test artifacts (failure screenshots,
# Playwright reports) — never fixtures, always regenerable.
tests/browser/__screenshots__
.vitest-attachments
test-results
playwright-report

# DONOR-001 R&D campaigns (research/donor-001-campaign*): isolated,
# research-only harnesses per their own REPORT.md — "no imports from or
# writes to src/**... nothing was committed or merged." Includes GGUF
# model weights and llama.cpp binaries (hundreds of MB); never belongs
# in repo history.
research/
